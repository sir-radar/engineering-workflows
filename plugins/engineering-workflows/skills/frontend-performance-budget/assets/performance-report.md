# Frontend performance report

- Code revision and dirty state:
- Production build command:
- Routes, journeys, fixtures, cache state, browser, host, and throttling:
- Existing or default budgets used:
- Field data source, window, segmentation, and p75:
- Five Lighthouse runs and median:
- Route asset list and compressed totals:
- Duplicate packages, images, and fonts:
- Long tasks, rendering profile, and layout shifts:
- Before/after result with observed variance:
- Accepted tradeoffs, overrides, blockers, and follow-ups:
