interface:
  display_name: "Caveman"
  short_description: "Compress replies without losing technical accuracy"
  default_prompt: "Use $caveman to answer tersely while preserving full technical accuracy."
policy:
  allow_implicit_invocation: true
