{
  "name": "engineering-workflows",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-toolkit.mjs",
    "check": "node --test && node scripts/validate-toolkit.mjs"
  }
}
